package hackerone

import "github.com/kenjoe41/h1scope/pkg/options"

func ProcessScope(scope *Scope, opt options.Options) *Scope {
	// Consider using a chan to write output.

	if scope == nil {
		return nil
	}

	return scope
}
